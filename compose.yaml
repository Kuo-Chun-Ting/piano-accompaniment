services:
  web:
    build: .
    init: true
    ports:
      - "127.0.0.1:${PORT:-3200}:3200"
    volumes:
      - audio-score-data:/app/.data
      - audio-score-models:/runtime/models
    restart: unless-stopped

volumes:
  audio-score-data:
  audio-score-models:
